/* global document */

import React from 'react';
import ReactDOM from 'react-dom';
import Options from "../../app/containers/Options";

ReactDOM.render(<Options />, document.getElementById('display-container'));
